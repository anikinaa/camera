import {config} from './config'export const Modal = ({index, onClose}) => {    return (        <div className={'modal'} style={{            display: index === null ? 'none' : 'block'        }}>            <p className={'modal-title'}>{config[index]?.title}</p>            <div className="wrap-modal-img">                <img className={'modal-img'} src={`images/${index}.jpg`} alt=""/>            </div>            <p dangerouslySetInnerHTML={{__html: config[index]?.text}}/>            <button className={'back'} onClick={onClose}>Назад</button>        </div>    )}